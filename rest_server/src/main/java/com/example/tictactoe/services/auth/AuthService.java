package com.example.tictactoe.services.auth;

import com.example.tictactoe.model.PlayersManager;

public class AuthService {
    public AuthResult register(String name) {
        PlayersManager playersManager = PlayersManager.getInstance();
    }
}
